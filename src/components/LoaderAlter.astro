<div class="loader"></div>

<style>
  .loader {
    width: 20px;
    aspect-ratio: 1;
    border-radius: 50%;
    background: #66ff00;
    box-shadow: 0 0 0 0 rgba(6, 184, 0, 0.715);
    animation: l2 1.5s infinite linear;
    position: relative;
  }
  .loader:before,
  .loader:after {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: inherit;
    box-shadow: 0 0 0 0 rgba(0, 95, 27, 0.267);
    animation: inherit;
    animation-delay: -0.5s;
  }
  .loader:after {
    animation-delay: -1s;
  }
  @keyframes l2 {
    100% {
      box-shadow: 0 0 0 40px #1fd91500;
    }
  }
</style>
